# AWS Configuration
# These values will be automatically populated from AWS CloudFormation outputs
# DO NOT commit the actual .env file - it's in .gitignore

# Database Configuration (retrieved from AWS Secrets Manager)
DB_HOST=
DB_PORT=5432
DB_NAME=rapidphoto
DB_USERNAME=
DB_PASSWORD=

# Redis Configuration
REDIS_HOST=
REDIS_PORT=6379

# S3 Configuration
AWS_REGION=us-east-1
S3_BUCKET=
S3_THUMBNAIL_BUCKET=

# JWT Configuration (generate with: openssl rand -base64 64)
JWT_SECRET=

# Frontend URL
FRONTEND_URL=
